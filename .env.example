# ====================================
# ARQUIVO DE EXEMPLO - NÃO USE EM PRODUÇÃO
# ====================================
#
# Este é um arquivo TEMPLATE. Para usar:
# 1. Copie este arquivo: cp .env.example .env
# 2. Edite o arquivo .env com seus valores reais
# 3. NUNCA faça commit do arquivo .env (já está no .gitignore)
#
# ====================================

# Configurações do Servidor
NODE_ENV=development
PORT=3000

# Segurança - Sessão
# IMPORTANTE: Gere um novo secret aleatório com o comando:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# NÃO USE O VALOR ABAIXO - É APENAS UM EXEMPLO!
SESSION_SECRET=GERE_UM_NOVO_SECRET_AQUI_COM_O_COMANDO_ACIMA

# Banco de Dados
DATABASE_PATH=./biblia.db

# ====================================
# Configurações para Produção
# ====================================
# Quando for para produção, altere:
# - NODE_ENV=production
# - Gere um novo SESSION_SECRET (diferente do desenvolvimento)
# - Configure as variáveis no servidor (Heroku, Railway, etc)
#   NÃO use arquivo .env em servidores de produção!
